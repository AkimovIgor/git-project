git init                                                                                  // Инициализация проекта (директории)
git config --global user.name "igor"                                       // Конфигурация и установка имени пользователя
git config --global user.email "akimow5417@gmail.com"         // Конфигурация и установка email пользователя
git config --list                                                                     // Получит список конфигурации

// Для добавления каталогов и файлов в список игнорируемых нужно создать в корне файл .gitignore.
// В этом файле нужно указать, какие каталоги или файлы будут проигнорированы, например:
	
	logs/
	docs/*.txt


git status                                                                              // Показывает добавленные (измененные) файлы и папки
git status --untracked-files=all                                              // Более подробно показывает добавленные (измененные) файлы и папки
git add .                                                                                // Подготавливает добавленные/измененные файлы перед коммитом
git rm --cached index.php                                                      // Удаляет файл из списка подготовленных для коммита
git commit -a -m"init"                                                             // Коммит 